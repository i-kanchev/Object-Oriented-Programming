#pragma once

const size_t MAX_TAG_SIZE = 10;
const size_t MAX_CHAR_SIZE = 25;
const size_t MAX_FILE_NAME_SIZE = 64;
const size_t MAX_BUFFER_SIZE = 1024;
const size_t STUDENTS_FIELDS = 6;

const unsigned MIN_AGE = 15;
const unsigned MAX_AGE = 65;
const float MIN_GRADE = 2.00;
const float MAX_GRADE = 6.00;

const unsigned CASE_NUMBER = -1; //underflow
